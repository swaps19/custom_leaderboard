<div class="row mb-3">
  <div class="col-lg-12">
    <% flash.each do |key, value| %>
      <div class="<%= flash_class(key.to_sym) %>">
            <%= value.html_safe %>
      </div>
    <% end %>
  </div>
</div>